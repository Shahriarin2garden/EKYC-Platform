# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm ci --omit=dev"]

[phases.build]
cmds = ["echo 'No build step needed for Node.js backend'"]

[start]
cmd = "cd backend && node src/server.js"

[variables]
NODE_ENV = "production"
